[build]
  command = "# no build command" # 빌드 명령어가 필요 없는 경우
  publish = "public"            # public 폴더를 웹사이트 루트로 사용

# .well-known 폴더와 pi.toml 파일에 대한 헤더 설정
[[headers]]
  for = "/.well-known/pi.toml"
  [headers.values]
    Content-Type = "application/toml"
    Access-Control-Allow-Origin = "*"
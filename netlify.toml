[build]
  # 넷리파이가 빌드 명령어를 실행할 위치입니다.
  base = "xpaio" 
  # 실행할 빌드 명령어입니다. (보내주신 package.json의 build 스크립트 실행)
  command = "npm run build"
  # Next.js 빌드 결과물이 나오는 폴더입니다.
  publish = ".next"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200